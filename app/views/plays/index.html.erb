<h1>Plays</h1>
<div class="container">
  <div class="row">
    <div class="col-md-4">
      <% @plays.each do |play| %>
      <h2><%= link_to play.title, play_path(play) %></h2>
      <% end %>
    </div>
  </div>
</div>

<% link_to "Add PLay", new_play_path %>
